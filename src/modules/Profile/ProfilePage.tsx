import Typography from '@mui/material/Typography'
type ProfilePageType = {
};

export const ProfilePage: React.FC<ProfilePageType> = ({ }) => {

    return (
        <>
            <Typography variant="h5">Profile</Typography>
        </>
    );
};