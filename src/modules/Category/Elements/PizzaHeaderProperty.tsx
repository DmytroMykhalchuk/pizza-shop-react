import { styled, Typography } from "@mui/material";

export const PizzaHeaderProperty = styled(Typography)(() => ({
    textAlign: 'center',
    justifyContent: 'center',
    alignItems: 'center',
    display: 'flex',
    flex: 1,
}));