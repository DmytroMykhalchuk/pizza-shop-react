import { Box, styled } from "@mui/material";

export const BannerImage = styled(Box)(({ }) => ({
    width: '100%',
    objectFit: 'cover',
    borderRadius: 12,
    height: '200px !important',
    marginBottom: 20,
}));

