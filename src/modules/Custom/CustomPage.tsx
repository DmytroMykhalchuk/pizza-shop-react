import { Typography } from "@mui/material";

type CustomPageType = {
};

export const CustomPage: React.FC<CustomPageType> = ({ }) => {

    return (
        <>
            <Typography variant="h5">Custom</Typography>
        </>
    );
};