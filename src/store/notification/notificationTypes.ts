export type NotificationType = {
    id: number;
    message: string;
    isChecked: boolean;
    type: string;
    createdAt: string;
};

export type FullNotificationType = {
    id: number;
    message: string;
    isChecked: boolean;
    type: string;
    createdAt: string;
};